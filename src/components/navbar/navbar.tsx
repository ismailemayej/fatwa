"use client";
import React from "react";
import ServerNavbar from "./ServerNavbar";
const navbar = () => {
  return (
    <div>
      <ServerNavbar user={""} />
    </div>
  );
};

export default navbar;
